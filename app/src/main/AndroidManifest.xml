<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hacktyki.car">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminPanel.AdminPanelAllReservation" />
        <activity android:name=".AdminPanel.AdminPanelMainAcitivity" />
        <activity android:name=".UserPanel.MyCarsActivity" />
        <activity android:name=".UserPanel.CurrentCarActivity" />
        <activity android:name=".AdminPanel.AdminPanelActivity" />
        <activity android:name=".LoginSignUp.SignUp.SignUpActivity" />
        <activity android:name=".UserPanel.HomeActivity" />
        <activity android:name=".LoginSignUp.Login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>